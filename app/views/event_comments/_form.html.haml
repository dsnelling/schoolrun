.schoolrun_form
  %fieldset
    %legend Please update your comment
    = form_for(@event_comment) do |f|
      - if @event_comment.errors.any?
        .error_explanation
          %h2
            = pluralize(@event_comment.errors.count, "error")
            prohibited this comment from being saved:

          %ul
            - @event_comment.errors.full_messages.each do |msg|
              %li= msg

      .field
        = f.label :comment
        = f.text_area :comment, :rows => 4
      .actions
        = f.submit
  .form_instruction
    To add some formatting to the comments: use "*" for bullets, *bold*, -underline-, _italics_, ["links":/url]
