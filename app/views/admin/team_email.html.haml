%h3 Team email

%p
  Emails will be sent to groups. You can use 'textile' to give some simple
  formatting to your message.
.schoolrun_form
  = form_tag("/team_email", :method => "post") do
    %fieldset
      .field
        = label_tag(:parent_email, "Parents:")
        = check_box_tag(:parent_email)
      .field
        = label_tag(:student_email, "Students:")
        = check_box_tag(:student_email)
      .field
        = label_tag(:admin_email, "Admins:")
        = check_box_tag(:admin_email)
      .field
        = label_tag(:message, "Message:")
        = text_area_tag(:message, "Dear ----- (enter your message..)", :size => "35x6")
      .actions
        = submit_tag("Send")

