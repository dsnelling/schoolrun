%h1 Bulk Import of Events

%p
  Upload file with many events;
  file format is "CSV"; need first line as header.

%pre
  title,description,to_from,location,event_date,event_time,status
  Netball Pickup,blah blah blah,From,LEH,2012-11-1,16:05,
  Swimming,LIV Early Morning Swimming,To,LEH,2012-11-17,07:30,
  \.....
 
.schoolrun_form

  = form_tag({:action => :create}, :multipart => true) do
    = file_field_tag :csv_file
    %br/
    = submit_tag 'Upload'


